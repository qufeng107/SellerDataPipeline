"""Local ingestion preparation helpers.

The ingestion package contains database-facing mapping logic, but the first stage intentionally
supports dry-run preview files without requiring Azure SQL to exist yet.
"""

# Integration tests

Integration tests may require real Azure SQL, Amazon SP-API, Amazon Ads API, or email credentials.

Do not run them automatically in CI unless the required secrets are configured and the cost/rate-limit impact is understood.
